/**
 * An apex page controller that exposes the site login functionality
 */
global with sharing class SiteLoginController {
   static testmethod void test()
   {
        new SiteLoginController();
   }
}